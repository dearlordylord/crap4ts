# Native package workspaces declare their publish-time OS/CPU filters. npm
# otherwise rejects non-host workspace links before optional dependency rules
# can apply; published consumers do not use this repository configuration.
force=true

{
  "name": "crap4ts-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Development workspace for the crap4ts Rust CLI and npm distribution",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19.0 <25"
  },
  "scripts": {
    "check:versions": "node scripts/check-versions.js",
    "check:schemas": "node scripts/check-schemas.js",
    "lint:npm": "node --check packages/crap4ts/bin/crap4ts.js && node --check packages/crap4ts/test/npm.test.js && node --check scripts/check-versions.js && node --check scripts/check-schemas.js && node --check scripts/validate-report.js && node --check scripts/stage-platform.js && node --check scripts/pack-platform.js && node --check scripts/npm-smoke.js && node --check scripts/release.js && node --check scripts/release-gate.js && node --check scripts/local-release.js",
    "lint": "npm run lint:npm",
    "pack:npm": "node scripts/pack-platform.js",
    "stage:npm": "node scripts/stage-platform.js",
    "smoke:npm": "node scripts/npm-smoke.js",
    "release:assemble": "node scripts/release.js assemble",
    "release:verify": "node scripts/release.js verify",
    "release:gate": "node scripts/release-gate.js",
    "local-release": "node scripts/local-release.js",
    "test:npm": "npm run test --workspace packages/crap4ts",
    "test": "npm run check:versions && npm run check:schemas && npm run test:npm && node --test scripts/*.test.js"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "fast-check": "4.9.0"
  }
}
